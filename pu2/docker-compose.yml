services:
  orchestrator:
    build: .
    container_name: orchestrator_node
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - soa_network

networks:
  soa_network:
    driver: bridge
